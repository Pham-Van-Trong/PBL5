Hệ thống nhận diện khuôn mặt được phát triển dựa trên ngôn ngữ lập trình Python

Trong hệ thống tích hợp nhận diện qua ESP32-cam để lấy hình ảnh trực tiếp, thông qua xử lý nhận diện để trả về 
kết quả đúng hay sai (có phải người quen trong bộ cơ sở dữ liệu đã học) để từ đó gọi API từ back-end xử lý phần cứng 
đóng mở cửa 